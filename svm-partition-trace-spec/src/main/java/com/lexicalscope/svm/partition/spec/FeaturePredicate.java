package com.lexicalscope.svm.partition.spec;

import com.google.common.base.Predicate;

public class FeaturePredicate<T1, T2> implements Predicate<T1> {
   public FeaturePredicate(final Predicate<Receiver> receiver) {
      // TODO Auto-generated constructor stub
   }

   @Override public boolean apply(final T1 input) {
      return false;
   }
}
